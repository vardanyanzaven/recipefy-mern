import { Box } from "@mui/material";
import styled from "styled-components";

export const BriefInfoBox = styled(Box)`
  width: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;
